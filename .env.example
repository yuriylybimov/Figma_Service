# URL of the Figma file the bridge will target. Create an empty design file
# during `python run.py login`, then paste its full URL here.
FIGMA_FILE_URL=Figma_url_here

# Firefox persistent profile directory. Stays gitignored.
PROFILE_DIR=./profile
